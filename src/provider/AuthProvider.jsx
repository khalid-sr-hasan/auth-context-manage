const AuthProvider = () => {
    const [user, setUser] = useState({});

    const authInfo = {
        register,
    };

    const register = (email, password) => {};

    return <div></div>;
};

export default AuthProvider;
